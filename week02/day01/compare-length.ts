'use strict';

let firstList: number[] = [1, 2, 3];
let secondList: number[] = [4, 5];

if (secondList.length > firstList.length) {
  console.log('Second List has more element then First List');
} else {
  console.log('First List has no less element then Second List');
}
